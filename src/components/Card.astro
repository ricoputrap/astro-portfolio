<div class="card">
  <slot />
</div>

<style>
  .card {
    background: var(--color-background-primary);
    padding: 20px;
    width: 384px;

    border-radius: 20px;
    border-width: 4px;
    border-color: var(--color-border-primary);
    border-style: solid;
  }
</style>